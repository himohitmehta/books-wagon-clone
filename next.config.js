/** @type {import('next').NextConfig} */
const nextConfig = {
	reactStrictMode: true,
	images: {
		domains: ["images.unsplash.com", "d2g9wbak88g7ch.cloudfront.net"],
	},
};

module.exports = nextConfig;
